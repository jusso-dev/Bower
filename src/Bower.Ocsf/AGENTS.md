# OCSF normalisation instructions

Keep mappers pure and versioned. Prefer additive OCSF field extensions. Never
execute untrusted mapping scripts. Unknown source kinds must fail closed with an
explicit error rather than inventing fields.
